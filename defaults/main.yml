---
install_path: /opt/sliver
setup_systemd: false
sliver_user:
  username: "sliver"
  usergroup: "sliver"
  sudo: true
  shell: /bin/bash
  plugins:
    - name: golang
      version: '1.22.0'
      scope: "global"
